package: minify_images
version: '1.1.0-dev'
author: Friends Of REDAXO
supportpage: github.com/FriendsOfREDAXO/minify_images

page:
    title: 'translate:title'
    perm: minify_images[]
    pjax: true
    subpages:
        config: { title: 'translate:config', perm: minify_images[config] }
        
requires:
    redaxo: '^5.1'
